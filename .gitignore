# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node modules & Dist
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ---------------------------------
# Rust & Tauri (Added)
# ---------------------------------
# Rust build artifacts (These are huge, do not commit)
src-tauri/target/

# Tauri v2 generated bindings (Safe to ignore)
src-tauri/gen/

# ---------------------------------
# Binaries (External Executables)
# ---------------------------------
# Ignore all files inside the binaries folder (ffmpeg, whisper-cli)
src-tauri/binaries/*

# BUT! Keep the folder itself by allowing this specific file
!src-tauri/binaries/.gitkeep